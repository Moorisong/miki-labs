# ChicoRun Problem Agent Reference

## 04_chicorun_level_system.md

# 04. 치코런 레벨 점프 역효과 방지 및 레벨 시스템 명세

## 1. 문제 인식
초급 유저가 실수로(또는 호기심에) 고급 레벨로 이동한 후 우연히 1~2문제를 맞히면, 기존 `maxLevel`이 영구적으로 높아진다. 이로 인해 다시 초급 문제로 돌아왔을 때 본인의 실제 실력과는 무관하게 포인트 페널티(1P 지급 등)를 받게 되는 역효과가 발생한다.

## 2. 해결 전략: 레벨 분리 및 엄격한 검증
학생의 실제 학습 위치와 포인트 계산의 기준이 되는 검증된 실력을 완전히 분리한다.

- **현재 플레이 레벨 (`currentLevel`)**: 학생이 자유롭게 변경하고 이동할 수 있는 레벨.
- **검증된 최고 레벨 (`achievedMaxLevel`)**: 실제로 레벨을 '완주'하고 '실력'이 증명되었을 때만 올라가는 레벨. **포인트 페널티 계산의 기준점**이 된다.

## 3. 데이터 구조 변경 (Student Model)
- `currentLevel`: 현재 플레이 중인 레벨
- `achievedMaxLevel`: 실제로 검증된 최고 레벨 (페널티 기준)
- `currentLevelSolvedCount`: 현재 레벨에서 맞힌 문제 수
- `currentLevelTotalCount`: 현재 레벨에서 푼 전체 문제 수
- `currentLevelMaxStreak`: 현재 레벨에서의 최대 연속 정답 수
- `currentLevelCurrentStreak`: 현재 연속 정답 수

## 4. `achievedMaxLevel` 업데이트 규칙
레벨 클리어 시점에만 업데이트되며 조건:
1. 해당 레벨 전체 완주 (12~18문제)
2. 정확도 70% 이상 (고급 60% 이상)
3. 연속 정답 5회 이상 기록

---

## 03_chicorun_problem_spec.md

# 03. 치코런 문제 템플릿 생성 및 관리 명세

[... 03_chicorun_problem_spec.md 의 내용 생략 ...]

---

## AI 작업 지침

### 목적
- 고품질의 영어 문제를 레벨별로 자동 생성 및 관리함.
- **학생의 실제 실력을 검증된 최고 레벨(`achievedMaxLevel`)으로 관리하여 비정상적인 레벨 점프에 따른 포인트 페널티 부작용을 원천 차단함.**

### 작업 단계
1. **정적 데이터 생성**: `problemSeeder`를 통해 레벨별, 유형별 최소 문제 수를 준수하여 생성.
2. **검증**: 생성된 문제가 상기 명시된 6번 검증 규칙을 준수하는지 LLM을 활용해 자가 교정 및 필터링.
3. **DB 저장**: `Prisma` 또는 `Mongoose`를 사용하여 생성된 문제를 DB에 삽입.
4. **포인트 및 레벨 처리**: `achievedMaxLevel`을 기반으로 페널티를 계산하고, 레벨 클리어 시점에만 통계 데이터를 검증하여 `achievedMaxLevel`을 업데이트함.
5. **프론트엔드 연동**: 레벨 선택 시 상위 레벨 점프에 대한 경고 알림(Modal)을 구현하고, 학습 시 실시간으로 페널티 가부를 노출함.

### 주의사항
- **데이터 보존**: 기존의 `maxLevel` 필드는 `achievedMaxLevel`로 전환하되, 데이터 마이그레이션 시 기존 값을 보존함.
- **엄격한 레벨업**: 단순히 다음 문제로 넘어간다고 `achievedMaxLevel`이 올라가지 않음을 명심. 반드시 레벨 완주 + 실력 지표(정확도, 스트릭)가 충족되어야 함.
- **MZ 말투 유지**: `explanation`은 반드시 친근하고 직설적인 말투로 작성.
